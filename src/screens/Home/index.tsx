import React from 'react';

import { CardContainer } from '../../components';

import './styles.scss';

export const Home = () => {
  return (
    <div className="home-container">
      <CardContainer />
    </div>
  );
};
